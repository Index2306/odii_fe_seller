import styled from 'styled-components/macro';
import { ProductHeader as Header } from './Header';

const ProductPage = styled.div``;

const Product = {
  Page: ProductPage,
  Header,
};

export default Product;
